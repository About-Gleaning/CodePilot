写入当前 plan agent 会话的执行计划 Markdown。

使用规则：
- 只能由 `plan` agent 调用；其他 agent 调用会被拒绝。
- `content` 是完整计划 Markdown，会覆盖当前 session 已保存的计划文件。
- 计划写入运行目录中的当前 session 文件，不写入用户源码目录。
- 该工具用于沉淀最终执行计划，不用于修改项目代码。

适合场景：
- 需求澄清完成后，保存可交给 build agent 执行的实现计划。
- 需要把目标、关键改动、测试方案和默认假设记录为结构化 Markdown。
